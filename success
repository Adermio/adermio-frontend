<!DOCTYPE html>
<html lang="fr" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">

    <title>Paiement confirmé — Adermio Premium</title>
    <meta name="description" content="Votre paiement a été confirmé. Votre analyse premium Adermio est en cours de préparation.">
    <meta name="robots" content="noindex, nofollow">
    <link rel="canonical" href="https://adermio.com/success">
    <link rel="alternate" hreflang="fr" href="https://adermio.com/success">
    <link rel="alternate" hreflang="en" href="https://adermio.com/en/success">
    <link rel="alternate" hreflang="x-default" href="https://adermio.com/success">
    <meta property="og:title" content="Paiement confirmé — Adermio Premium">
    <meta property="og:description" content="Votre paiement a été confirmé. Votre analyse premium Adermio est en cours de préparation.">
    <meta property="og:url" content="https://adermio.com/success">
    <meta property="og:type" content="website">
    <meta property="og:image" content="https://adermio.com/og-image.png">
    <meta property="og:image:width" content="1200">
    <meta property="og:image:height" content="630">
    <meta property="og:locale" content="fr_FR">
    <meta property="og:site_name" content="Adermio">
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="Paiement confirmé — Adermio Premium">
    <meta name="twitter:description" content="Votre paiement a été confirmé. Votre analyse premium Adermio est en cours de préparation.">
    <meta name="twitter:image" content="https://adermio.com/og-image.png">
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="manifest" href="/site.webmanifest">
    <meta name="theme-color" content="#0F3D39">
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:opsz,wght@9..40,300;400;500;600;700&family=Playfair+Display:ital,wght@0,400;0,500;0,600;1,400&family=Manrope:wght@400;500;700;800&display=swap" rel="stylesheet">
    
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script src="https://unpkg.com/lucide@latest"></script>
    
    <script src="https://cdn.tailwindcss.com"></script>

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        adermio: {
                            base: '#F8FAFC',   
                            dark: '#0F3D39',   
                            primary: '#14B8A6',
                            text: '#334155',   
                            muted: '#94A3B8',  
                            surface: '#FFFFFF' 
                        }
                    },
                    fontFamily: {
                        serif: ['"Playfair Display"', 'serif'],
                        sans: ['"DM Sans"', 'sans-serif'],
                        manrope: ['"Manrope"', 'sans-serif'],
                    },
                    boxShadow: {
                        'soft': '0 30px 60px -15px rgba(15, 61, 57, 0.15)',
                        'luxury': '0 0 0 1px rgba(255,255,255,0.4) inset, 0 20px 40px -20px rgba(0,0,0,0.1)',
                    },
                }
            }
        }
    </script>

    <style>
        ::-webkit-scrollbar { width: 0px; background: transparent; }
        
        a, a:visited, a:hover, a:active {
            color: inherit; 
            text-decoration: none;
        }
        .text-adermio-dark { color: #0F3D39 !important; }
        
        .glass-nav {
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border-bottom: 1px solid rgba(15, 61, 57, 0.05);
        }

        #full-menu {
            transition: opacity 0.4s ease, visibility 0.4s;
            opacity: 0;
            visibility: hidden;
            pointer-events: none;
        }
        #full-menu.menu-active {
            opacity: 1;
            visibility: visible;
            pointer-events: auto;
        }

        /* Language Dropdown Styles */
        .lang-dropdown {
            opacity: 0;
            visibility: hidden;
            transform: translateY(10px);
            transition: all 0.2s ease;
        }
        .group:hover .lang-dropdown {
            opacity: 1;
            visibility: visible;
            transform: translateY(0);
        }
    </style>

    <style>
        /* --- DESIGN SYSTEM PREMIUM --- */
        :root {
            --primary-gold: #d97706; 
            --primary-dark: #0f172a; 
            --text-grey: #64748b;
            --bg-soft: #f8fafc;
            --white: #ffffff;
            --shadow-card: 0 20px 40px -10px rgba(0, 0, 0, 0.05);
            --radius-card: 24px;
            
            /* Nouvelle couleur pour le bouton style Adermio */
            --adermio-teal: #00897b; 
        }

        .main-container {
            font-family: 'Manrope', sans-serif; /* Force la police Manrope sur ton contenu */
            max-width: 680px;
            margin: 0 auto;
            padding: 60px 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
            min-height: 80vh; /* Ajusté pour bien s'intégrer avec le footer */
            color: var(--primary-dark);
        }

        /* 1. HEADER */
        .header-section {
            text-align: center;
            margin-bottom: 50px;
            animation: fadeIn 0.8s ease;
        }

        .icon-success-wrapper {
            width: 50px;
            height: 50px;
            background: #dcfce7;
            color: #15803d;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
            margin: 0 auto 20px auto;
            box-shadow: 0 10px 20px rgba(22, 163, 74, 0.1);
        }

        .main-title {
            font-size: 24px;
            font-weight: 800;
            color: var(--primary-dark);
            margin: 0 0 8px 0;
            letter-spacing: -0.5px;
        }
        
        .main-subtitle {
            font-size: 15px;
            color: var(--text-grey);
            font-weight: 500;
        }

        /* 2. CARTE D'ANALYSE */
        .analysis-card {
            background: var(--white);
            border-radius: var(--radius-card);
            box-shadow: var(--shadow-card);
            padding: 50px 40px;
            width: 100%;
            box-sizing: border-box;
            text-align: center;
            border: 1px solid #f1f5f9;
            margin-bottom: 50px;
            position: relative;
            overflow: hidden; 
        }

        /* LOADER */
        .loader-container {
            position: relative;
            width: 220px; 
            height: 220px; 
            margin: 0 auto 40px auto;
        }

        .status-text-title {
            font-size: 18px;
            font-weight: 700;
            color: var(--primary-dark);
            margin-bottom: 6px;
        }

        .status-text-sub {
            font-size: 14px;
            color: var(--text-grey);
            height: 20px;
            margin-bottom: 30px;
            transition: opacity 0.3s ease;
        }

        /* Pourcentage */
        .percentage-display {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            font-size: 36px; 
            font-weight: 800;
            color: var(--primary-dark);
            letter-spacing: -1px;
        }

        /* BARRE SECONDAIRE (Extension) */
        .extension-bar-wrapper {
            width: 160px;
            height: 4px;
            background: #f1f5f9;
            border-radius: 10px;
            margin: -20px auto 30px auto; /* Positionné sous le cercle */
            overflow: hidden;
            display: none; /* Caché par défaut */
            opacity: 0;
            transition: opacity 0.5s ease;
        }
        .extension-bar-fill {
            height: 100%;
            width: 0%;
            background: #d97706; /* Gold */
            border-radius: 10px;
            /* Transition définie en JS ou ici, on met 30s par défaut pour correspondre au code */
            transition: width 30s linear; 
        }

        /* --- BLOC EMAIL --- */
        .info-box-container {
            display: flex;
            flex-direction: column;
            gap: 15px;
            max-width: 90%;
            margin: 0 auto;
        }

        .download-hint {
            font-size: 12px;
            color: var(--primary-gold);
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 5px;
        }

        .email-assurance-box {
            background: #f8fafc;
            border: 1px solid #e2e8f0;
            border-radius: 16px;
            padding: 14px 20px;
            display: flex;
            align-items: center;
            gap: 16px;
            text-align: left;
        }

        .email-icon-circle {
            width: 36px;
            height: 36px;
            background: white;
            border: 1px solid #e2e8f0;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--primary-dark);
            font-size: 14px;
            flex-shrink: 0;
            box-shadow: 0 2px 5px rgba(0,0,0,0.02);
        }

        .email-text {
            font-size: 13px;
            color: #475569;
            line-height: 1.5;
            font-weight: 500;
        }

        /* SUCCÈS (État final) */
        .success-state {
            display: none;
            opacity: 0;
            transform: translateY(20px);
            transition: all 0.6s cubic-bezier(0.16, 1, 0.3, 1);
        }

        /* --- NOUVEAU BOUTON VIBE ADERMIO --- */
        .btn-premium-access {
            background-color: var(--adermio-teal); /* Couleur Teal Style Adermio */
            
            /* CORRECTION ICI : Ajout de !important pour forcer le blanc */
            color: #ffffff !important; 
            
            padding: 18px 45px;
            border-radius: 50px; 
            text-decoration: none;
            font-weight: 700;
            font-size: 16px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            box-shadow: 0 10px 25px -5px rgba(0, 137, 123, 0.4); 
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
        }
        
        .btn-premium-access:hover {
            background-color: #00796b; 
            transform: translateY(-3px);
            box-shadow: 0 15px 35px -5px rgba(0, 137, 123, 0.5);
            color: #ffffff !important; /* On force aussi au survol */
        }

        /* ICÔNE SUCCÈS (ÉTOILE / RÉVÉLATION) */
        .success-icon-container {
            font-size: 40px; 
            color: var(--primary-gold); 
            margin-bottom: 25px;
            animation: popIn 0.6s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        @keyframes popIn {
            0% { transform: scale(0); opacity: 0; }
            100% { transform: scale(1); opacity: 1; }
        }

        /* 3. FOOTER TRUST */
        .trust-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 20px;
            width: 100%;
            opacity: 0;
            animation: fadeIn 0.8s ease 0.5s forwards;
        }

        .trust-item {
            text-align: center;
            padding: 15px;
        }

        .trust-icon { font-size: 20px; color: var(--primary-gold); margin-bottom: 10px; display: block; opacity: 0.8; }
        .trust-title { font-size: 11px; font-weight: 800; color: var(--primary-dark); margin-bottom: 5px; text-transform: uppercase; letter-spacing: 1px; }
        .trust-desc { font-size: 11px; color: var(--text-grey); line-height: 1.4; }

        @keyframes fadeIn { from {opacity:0; transform:translateY(10px);} to {opacity:1; transform:translateY(0);} }
        
        /* --- TIMEOUT MODAL (Pop-up de secours) --- */
        .timeout-modal-overlay {
            position: fixed;
            top: 0; left: 0; right: 0; bottom: 0;
            background: rgba(15, 61, 57, 0.4); /* Dark teal overlay */
            backdrop-filter: blur(5px);
            z-index: 100;
            display: flex;
            align-items: center;
            justify-content: center;
            opacity: 0;
            visibility: hidden;
            transition: all 0.4s ease;
        }

        .timeout-modal-overlay.active {
            opacity: 1;
            visibility: visible;
        }

        .timeout-modal {
            background: white;
            padding: 30px;
            width: 90%;
            max-width: 400px;
            border-radius: 20px;
            text-align: center;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
            transform: scale(0.9);
            transition: transform 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        .timeout-modal-overlay.active .timeout-modal {
            transform: scale(1);
        }

        .modal-icon {
            font-size: 32px;
            color: var(--primary-gold);
            margin-bottom: 15px;
        }
        
        /* Responsive */
        @media (max-width: 600px) {
            .trust-grid { grid-template-columns: 1fr; gap: 10px; }
            .trust-item { display: flex; align-items: center; gap: 15px; text-align: left; padding: 10px; background: rgba(255,255,255,0.5); border-radius: 12px; }
            .trust-icon { margin-bottom: 0; }
            .analysis-card { padding: 40px 20px; }
            .loader-container { width: 200px; height: 200px; } 
            .percentage-display { font-size: 32px; }
            .btn-premium-access { width: 100%; justify-content: center; padding: 20px; }
        }
    </style>
</head>
<body class="bg-adermio-base text-adermio-text antialiased selection:bg-adermio-dark selection:text-white overflow-x-hidden pb-32">

    <nav class="fixed top-0 left-0 right-0 z-[60] glass-nav transition-all duration-300">
        <div class="max-w-7xl mx-auto px-6 py-4 flex justify-between items-center">
            <a href="/" class="flex items-center gap-2">
                 <img src="https://static.tildacdn.com/tild6132-3466-4462-b065-666134646636/logo_3.png" alt="Logo" class="h-8 w-auto object-contain">
                 <span class="font-serif text-xl text-adermio-dark font-semibold tracking-tight">Adermio</span>
            </a>

            <div class="flex items-center gap-2">
                
                <div class="relative group">
                    <button class="flex items-center gap-1.5 px-3 py-2 rounded-full hover:bg-black/5 transition-colors text-adermio-dark">
                        <img src="https://flagcdn.com/fr.svg" width="16" alt="FR" class="rounded-sm shadow-sm">
                        <span class="font-sans font-medium text-sm">FR</span>
                        <i data-lucide="chevron-down" class="w-3 h-3 opacity-50"></i>
                    </button>
                    <div class="lang-dropdown absolute right-0 top-full mt-2 w-36 bg-white rounded-xl shadow-luxury border border-adermio-dark/5 overflow-hidden z-[80]">
                        <a href="#" class="flex items-center gap-3 px-4 py-3 text-sm font-medium bg-teal-50/50 text-teal-700">
                            <img src="https://flagcdn.com/fr.svg" width="20" alt="Français" class="rounded-sm shadow-sm">
                            <span>Français</span>
                        </a>
                        <a href="https://adermio.com/en/finalisation" class="flex items-center gap-3 px-4 py-3 text-sm font-medium text-adermio-dark hover:bg-gray-50 transition-colors">
                            <img src="https://flagcdn.com/us.svg" width="20" alt="English" class="rounded-sm shadow-sm">
                            <span>English</span>
                        </a>
                    </div>
                </div>

                <button id="menu-btn" class="text-adermio-dark hover:text-teal-700 transition-colors p-2 z-[70]">
                    <i data-lucide="menu" class="w-6 h-6"></i>
                </button>
            </div>
        </div>
    </nav>

    <div id="full-menu" class="fixed inset-0 z-[55] bg-white/98 backdrop-blur-xl flex flex-col items-center justify-center">
        <nav class="flex flex-col items-center gap-8 md:gap-10">
            <a href="/" class="menu-link font-serif text-3xl md:text-5xl !text-adermio-dark hover:text-teal-600 transition-colors">Accueil</a>
            <a href="https://adermio.com/formulaire" class="menu-link font-serif text-3xl md:text-5xl !text-adermio-dark hover:text-teal-600 transition-colors">Faire l'analyse</a>
            <a href="https://adermio.com/about" class="menu-link font-serif text-3xl md:text-5xl !text-adermio-dark hover:text-teal-600 transition-colors">À Propos</a>
            <a href="https://adermio.com/contact" class="menu-link font-serif text-3xl md:text-5xl !text-adermio-dark hover:text-teal-600 transition-colors">Nous Contacter</a>
        </nav>
        <div class="absolute bottom-10 left-0 right-0 text-center">
            <span class="text-xs uppercase tracking-widest text-adermio-muted">Adermio © 2025</span>
        </div>
    </div>

    <main class="pt-20">
        
        <div class="main-container">

            <div class="header-section">
                <div class="icon-success-wrapper">
                    <i class="fa-solid fa-check"></i>
                </div>
                <h1 class="main-title">Commande validée</h1>
                <div class="main-subtitle">Merci. Votre dossier complet Adermio est en cours de création.</div>
            </div>

            <div class="analysis-card">
                
                <div id="loading-content">
                    <div id="status-title" class="status-text-title">Analyse dermatologique en cours...</div>
                    <div id="status-subtitle" class="status-text-sub">Initialisation de l'IA...</div>

                    <div class="loader-container">
                        <svg viewBox="0 0 100 100" style="transform:rotate(-90deg);width:100%;height:100%;">
                            <circle cx="50" cy="50" r="45" stroke="#f1f5f9" stroke-width="8" fill="none" />
                            <circle id="progress-circle" cx="50" cy="50" r="45" stroke="#d97706" stroke-width="8"
                                    fill="none" stroke-linecap="round"
                                    stroke-dasharray="282.6" stroke-dashoffset="282.6"
                                    style="transition: stroke-dashoffset 0.5s linear;" />
                        </svg>
                        <div id="progress-text" class="percentage-display">0%</div>
                    </div>

                    <div id="extension-bar-wrapper" class="extension-bar-wrapper">
                        <div id="extension-bar-fill" class="extension-bar-fill"></div>
                    </div>

                    <div class="info-box-container">
                        <div class="download-hint">
                            <i class="fa-solid fa-wand-magic-sparkles" style="margin-right:6px;"></i> Finalisation du dossier...
                        </div>
                        
                        <div class="email-assurance-box">
                            <div class="email-icon-circle">
                                <i class="fa-regular fa-envelope"></i>
                            </div>
                            <div class="email-text">
                                <strong>Copie de sécurité</strong><br>
                                Le rapport PDF sera envoyé automatiquement sur votre email.
                            </div>
                        </div>
                    </div>
                </div>

                <div id="success-state" class="success-state">
                    <div class="success-icon-container">
                        <i class="fa-solid fa-wand-magic-sparkles"></i>
                    </div>
                    
                    <h2 style="font-size: 26px; font-weight: 800; color: #0f172a; margin-bottom: 12px; letter-spacing: -0.5px;">Votre dossier est prêt</h2>
                    <p style="color: #64748b; font-size: 15px; margin-bottom: 40px; max-width: 420px; margin-left: auto; margin-right: auto; line-height: 1.6;">
                        L'analyse complète de votre peau est terminée. Vous pouvez consulter vos résultats dès maintenant.
                    </p>

                    <a id="access-btn" href="#" class="btn-premium-access">
                        Ouvrir mon analyse complète
                    </a>
                    
                    <div style="margin-top:30px; font-size:12px; color:#94a3b8; font-weight: 500;">
                        <i class="fa-solid fa-check" style="color:#16a34a; margin-right:6px;"></i> Copie PDF envoyée par email
                    </div>
                </div>

            </div>

            <div class="trust-grid" id="trust-grid">
                <div class="trust-item">
                    <i class="fa-solid fa-microscope trust-icon"></i>
                    <div>
                        <div class="trust-title">Scan HD</div>
                        <div class="trust-desc">Analyse pore par pore</div>
                    </div>
                </div>
                <div class="trust-item">
                    <i class="fa-solid fa-flask trust-icon"></i>
                    <div>
                        <div class="trust-title">Méthode Clinique</div>
                        <div class="trust-desc">IA Adermio Lab</div>
                    </div>
                </div>
                <div class="trust-item">
                    <i class="fa-solid fa-lock trust-icon"></i>
                    <div>
                        <div class="trust-title">Données Chiffrées</div>
                        <div class="trust-desc">100% Confidentiel</div>
                    </div>
                </div>
            </div>

        </div>
    </main>

    <div id="timeout-modal" class="timeout-modal-overlay">
        <div class="timeout-modal">
            <div class="modal-icon">
                <i class="fa-regular fa-circle-question"></i>
            </div>
            <h3 class="text-xl font-bold text-adermio-dark mb-3">Une petite minute...</h3>
            <p class="text-sm text-gray-500 mb-6 leading-relaxed">
                Vous n'avez pas reçu votre analyse ? Aucun problème. Notre serveur est peut-être surchargé. <br>Envoyez-nous un message et nous vous la renverrons immédiatement par mail.
            </p>
            <a href="https://adermio.com/contact" class="inline-flex items-center justify-center w-full px-6 py-3 text-sm font-bold text-teal-700 bg-teal-50 rounded-xl hover:bg-teal-100 transition-colors">
                Contacter le support
            </a>
            <button onclick="document.getElementById('timeout-modal').classList.remove('active')" class="mt-4 text-xs text-gray-400 underline hover:text-gray-600">
                Continuer d'attendre
            </button>
        </div>
    </div>

    <footer class="bg-white border-t border-adermio-dark/5 py-16 px-6">
        <div class="max-w-4xl mx-auto flex flex-col items-center text-center">
            
            <div class="w-24 h-24 mb-6 relative flex items-center justify-center p-4 rounded-full bg-adermio-base border border-adermio-dark/5 z-10">
                <img src="https://static.tildacdn.com/tild6132-3466-4462-b065-666134646636/logo_3.png" 
                     alt="Adermio Logo" 
                     class="w-full h-full object-contain relative z-20" loading="lazy">
            </div>

            <span class="font-serif text-2xl font-bold text-adermio-dark mb-2">Adermio</span>
            <p class="text-sm text-adermio-text/60 max-w-sm mb-10 font-light">
                La dermatologie réinventée par l'intelligence artificielle.
            </p>

            <div class="flex flex-wrap justify-center gap-x-8 gap-y-4 mb-8 text-sm font-medium text-adermio-dark/90">
                <a href="/" class="hover:text-teal-600 transition-colors !text-adermio-dark">Accueil</a>
                <a href="https://adermio.com/formulaire" class="hover:text-teal-600 transition-colors !text-adermio-dark">Faire l'analyse</a>
                <a href="https://adermio.com/about" class="hover:text-teal-600 transition-colors !text-adermio-dark">À Propos</a>
                <a href="https://adermio.com/contact" class="hover:text-teal-600 transition-colors !text-adermio-dark">Nous Contacter</a>
            </div>

            <div class="flex flex-wrap justify-center gap-x-6 gap-y-2 mb-10 text-xs uppercase tracking-wider">
                <a href="https://www.adermio.com/conditions" class="!text-slate-500 hover:!text-adermio-dark transition-colors">Conditions d'utilisation</a>
                <a href="https://www.adermio.com/mentions-legales" class="!text-slate-500 hover:!text-adermio-dark transition-colors">Mentions Légales</a>
                <a href="https://www.adermio.com/confidentialite" class="!text-slate-500 hover:!text-adermio-dark transition-colors">Politique de Confidentialité</a>
            </div>

            <div class="text-xs text-adermio-muted/40 font-light">
                © 2025 Adermio Lab. Tous droits réservés.
            </div>
        </div>
    </footer>

    <script>
        lucide.createIcons();

        // Logique du Menu
        const menuBtn = document.getElementById('menu-btn');
        const menuOverlay = document.getElementById('full-menu');
        const menuLinks = document.querySelectorAll('.menu-link');
        let isMenuOpen = false;

        function toggleMenu() {
            isMenuOpen = !isMenuOpen;
            if (isMenuOpen) {
                menuOverlay.classList.add('menu-active');
                menuBtn.innerHTML = '<i data-lucide="x" class="w-6 h-6"></i>';
                menuBtn.classList.add('text-teal-900');
            } else {
                menuOverlay.classList.remove('menu-active');
                menuBtn.innerHTML = '<i data-lucide="menu" class="w-6 h-6"></i>';
                menuBtn.classList.remove('text-teal-900');
            }
            lucide.createIcons();
        }

        menuBtn.addEventListener('click', (e) => {
            e.stopPropagation();
            toggleMenu();
        });

        menuLinks.forEach(link => {
            link.addEventListener('click', () => {
                if(isMenuOpen) toggleMenu();
            });
        });
    </script>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
       
          // 1. RECUPERATION JOB ID
          const params = new URLSearchParams(window.location.search);
          const jobId = params.get("jobId") || params.get("client_reference_id");
       
          if (!jobId) {
              document.getElementById("status-subtitle").innerText = "Erreur : Commande introuvable.";
              document.getElementById("status-subtitle").style.color = "#ef4444";
              return; 
          }
       
          // --- CONFIGURATION ---
          const POLL_URL = "https://n8n.adermio.com/webhook/aae2ff2f-cd35-48d0-b0d2-f56d2341452a?jobId="; 
          
          // 1. Durée chargement normal : 90 secondes
          const MAIN_LOAD_TIME = 90 * 1000; 
          
          // 2. Durée Extension : 30 secondes
          const EXTENSION_TIME = 30 * 1000;

          // 3. Buffer final avant l'arrêt complet (15 secondes)
          const EXTRA_BUFFER_TIME = 15 * 1000;
          
          const POLL_INTERVAL = 3000; 
          
          const loadingSteps = [
              { pct: 0,  text: "Connexion sécurisée au serveur..." },
              { pct: 20, text: "Traitement haute définition des images..." },
              { pct: 40, text: "Analyse des imperfections et du grain..." },
              { pct: 60, text: "Génération du protocole sur-mesure..." },
              { pct: 85, text: "Construction de l'interface..." },
              { pct: 95, text: "Finalisation du dossier..." }
          ];
       
          const startTime = Date.now();
          const progressCircle = document.getElementById("progress-circle");
          const progressText   = document.getElementById("progress-text");
          const circumference  = 2 * Math.PI * 45; 
          
          const loadingContent = document.getElementById("loading-content");
          const successState   = document.getElementById("success-state");
          const accessBtn      = document.getElementById("access-btn");
          const subtitle       = document.getElementById("status-subtitle");
          const trustGrid      = document.getElementById("trust-grid");
          
          // Elements pour l'extension et le timeout
          const extensionBarWrapper = document.getElementById("extension-bar-wrapper");
          const extensionBarFill    = document.getElementById("extension-bar-fill");
          const timeoutModal        = document.getElementById("timeout-modal");
       
          let progressInterval;
          let pollingInterval;
          let isSuccess = false;
          let isExtendedPhase = false;
          let hasTriggeredTimeout = false;
       
          // A. Animation Visuelle du Cercle (0 -> 60s)
          function animateProgress() {
            progressInterval = setInterval(() => {
              const elapsed  = Date.now() - startTime;
              
              // On calcule le pourcentage sur la base de MAIN_LOAD_TIME (60s)
              // On bloque à 99%
              const progress = Math.min((elapsed / MAIN_LOAD_TIME) * 100, 99);
              
              if (isSuccess) return;
       
              // MISE A JOUR UI CERCLE
              const offset   = circumference - (progress / 100) * circumference;
              if(progressCircle) progressCircle.style.strokeDashoffset = offset.toFixed(2);
              if(progressText) progressText.textContent = Math.floor(progress) + "%";
       
              // MISE A JOUR TEXTES ETAPES
              const currentStep = loadingSteps.slice().reverse().find(step => progress >= step.pct);
              if (currentStep && subtitle && subtitle.innerText !== currentStep.text && !isExtendedPhase) {
                    subtitle.style.opacity = 0; 
                    setTimeout(() => {
                        if(!isExtendedPhase) subtitle.innerText = currentStep.text;
                        subtitle.style.opacity = 1;
                    }, 300);
              }
              
              // FIN DU CERCLE (60s) -> LANCEMENT BARRE HORIZONTALE
              if (elapsed >= MAIN_LOAD_TIME && !isSuccess && !isExtendedPhase) {
                  startExtensionPhase();
              }

            }, 100);
          }

          // B. Phase d'extension (Barre horizontale - 30 secondes)
          function startExtensionPhase() {
              isExtendedPhase = true;
              clearInterval(progressInterval); // On arrête le cercle à 99%

              // Mise à jour visuelle pour l'extension
              subtitle.style.opacity = 0;
              setTimeout(() => {
                  subtitle.innerText = "Trafic plus dense que prévu : finalisation en cours...";
                  subtitle.style.color = "#d97706"; // Gold color
                  subtitle.style.opacity = 1;
              }, 300);

              // Affichage et animation de la nouvelle barre
              extensionBarWrapper.style.display = "block";
              setTimeout(() => {
                  extensionBarWrapper.style.opacity = 1;
                  extensionBarFill.style.width = "100%"; // Déclenche la transition CSS de 30s
              }, 100);

              // Quand la barre a fini de charger (30s plus tard), on lance le buffer final
              setTimeout(() => {
                  if (!isSuccess && !hasTriggeredTimeout) {
                      startFinalBufferPhase();
                  }
              }, EXTENSION_TIME);
          }

          // C. Buffer Final (15 secondes supplémentaires "au cas où")
          function startFinalBufferPhase() {
              // On ne change pas l'UI ici, on laisse juste le polling tourner 15s de plus
              // Total écoulé ici : 60s + 30s = 1m30s
              
              setTimeout(() => {
                  if (!isSuccess && !hasTriggeredTimeout) {
                      showTimeoutPopup(); // Stop polling se fait ici
                  }
              }, EXTRA_BUFFER_TIME);
          }

          // D. Affichage du Popup Timeout & ARRÊT DU POLLING
          function showTimeoutPopup() {
              hasTriggeredTimeout = true;
              stopPolling(); // <--- ARRÊT IMPERATIF DES APPELS RESEAU
              timeoutModal.classList.add('active');
          }
       
          // E. Polling
          async function checkStatus() {
            try {
              if (isSuccess) return;

              const res = await fetch(POLL_URL + encodeURIComponent(jobId), { method: "GET", cache: "no-store" });
              const data = await res.json();
              if (!data) return;
              
              const record = Array.isArray(data) ? data[0] : data;
       
              // Condition "ROBUSTE"
              if (record && record.downloadUrl && record.downloadUrl.length > 10) {
                isSuccess = true;
                stopPolling();
                showSuccess(record.downloadUrl);
              }
            } catch (e) { console.error("Polling...", e); }
          }
       
          // F. Succès
          function showSuccess(url) {
              // Fermer le modal de timeout s'il est ouvert
              if(timeoutModal.classList.contains('active')) {
                  timeoutModal.classList.remove('active');
              }

              // Remplir le cercle
              if(progressText) progressText.textContent = "100%";
              if(progressCircle) {
                  progressCircle.style.strokeDashoffset = 0;
                  progressCircle.style.stroke = "#16a34a"; // Vert succès
              }

              // Cacher la barre d'extension si elle était là
              if(isExtendedPhase) {
                   extensionBarWrapper.style.opacity = 0;
              }
       
              setTimeout(() => {
                  loadingContent.style.opacity = 0;
                  loadingContent.style.transition = "opacity 0.5s ease";
                  
                  setTimeout(() => {
                      loadingContent.style.display = "none";
                      accessBtn.href = url;
                      
                      successState.style.display = "block";
                      setTimeout(() => {
                          successState.style.opacity = 1;
                          successState.style.transform = "translateY(0)";
                      }, 50);

                      if(trustGrid) trustGrid.style.opacity = 0.6;
                  }, 500);
              }, 600);
          }
       
          function stopPolling() {
            clearInterval(pollingInterval);
            clearInterval(progressInterval);
          }
       
          // Démarrage
          animateProgress();
          setTimeout(() => {
            checkStatus(); 
            pollingInterval = setInterval(() => checkStatus(), POLL_INTERVAL);
          }, 2500);
       
          window.addEventListener('beforeunload', () => stopPolling());
        });
      </script>
</body>
</html>
